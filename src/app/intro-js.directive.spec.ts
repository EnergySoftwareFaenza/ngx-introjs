import { IntroJsDirective } from './intro-js.directive';

describe('IntroJsDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new IntroJsDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
